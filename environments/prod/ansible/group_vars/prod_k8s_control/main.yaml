---
# Production Kubernetes Control Plane Configuration
# Variables specific to control plane nodes

# Node Role Configuration
cluster_role: "control-plane"
node_type: "talos"

# Control Plane Specific Settings
control_plane_high_availability: true
control_plane_backup_enabled: true
