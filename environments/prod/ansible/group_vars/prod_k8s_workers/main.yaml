---
# Production Kubernetes Workers Configuration
# Variables specific to worker nodes

# Node Role Configuration
cluster_role: "worker"
node_type: "talos"

# Worker Specific Settings
worker_auto_scaling_enabled: true
worker_resource_limits_enabled: true
